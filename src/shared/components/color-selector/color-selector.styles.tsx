import { StyleSheet } from 'react-native';

const ColorSelectorStyles = StyleSheet.create({
  container: {
    marginTop: 16,
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    alignItems: 'center',
    flexWrap: 'wrap'
  },
});

export default ColorSelectorStyles;
