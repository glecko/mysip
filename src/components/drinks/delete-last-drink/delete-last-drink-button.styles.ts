import { StyleSheet } from 'react-native';

export const DeleteLastDrinkButtonStyles = StyleSheet.create({
  container: {
    position: 'absolute',
    left: 20,
    top: 20,
  },
  fabButton: {
    backgroundColor: 'orange'
  },
});
