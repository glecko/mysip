import { StyleSheet } from 'react-native';

const HealthSettingsDialogStyles = StyleSheet.create({
  container: {},
  genderSelectorContainer: {
    marginTop: 16,
  }
});

export default HealthSettingsDialogStyles;
